---
import Layout from '@/layouts/Layout.astro'
import { Header } from '@/components/header/Header'
import Footer from '@/components/footer/Footer.astro'
import { Provider } from '@/components/provider/Provider'
import TravelMap from '@/components/gallery/TravelMap'
import travelData from '@/gallery.json'
import Highlight from '@/components/Highlight.astro'


interface Props {
  title?: string
  description?: string
  image?: string
}

const title = '时光廊'
const { description, image } = Astro.props
---

<Layout title={title} description={description} image={image}>
  <Provider pathName={Astro.url.pathname} client:only="react" />

  <Header client:only="react" />
  <!-- <main class="relative z-1 pt-16 min-h-main bg-primary">
    <div class="swup-transition-fade">
      <h1 class="text-3xl font-bold mb-4">{title}</h1>
      <TravelMap travelData={travelData} client:only="react" />
      <h1 class="text-3xl font-bold mb-4">旅行足迹</h1>
      <h1 class="text-3xl font-bold mb-4">旅行足迹</h1>
      <h1 class="text-3xl font-bold mb-4">旅行足迹</h1>
      <slot />
    </div>
  </main> -->
  <main class="relative z-1 pt-16 min-h-main bg-primary">
    <div class="swup-transition-fade max-w-[800px] mx-auto px-4 py-16 space-y-8">
      <!-- 页面标题和描述 -->
      <header class="space-y-4">
        <h1 class="text-4xl font-bold">
          <Highlight>{title}</Highlight>
        </h1>
      </header>

      <!-- TravelMap 组件 -->
      <!-- <div data-swup-ignore>
        <TravelMap travelData={travelData} client:only="react" />
      </div> -->

      <!-- 你可以在这里添加其他时光廊内容 -->
      <section class="timeline mt-12 space-y-8">
        <div class="timeline-item">
          <h2 class="text-2xl font-semibold">建设中...</h2>
          <!-- <p>建设中</p> -->
        </div>
        <!-- <div class="timeline-item">
          <h2 class="text-2xl font-semibold">2025年2月</h2>
          <p>更多内容...</p>
        </div> -->
      </section>

      <slot />
    </div>
  </main>
  <Footer />
</Layout>